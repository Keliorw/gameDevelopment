using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.SceneManagement;

public class StartupManager : MonoBehaviour
{
    /*
    private IEnumerator Start()
    {
        Debug.Log("Before while" + LocalizationManager.instance.GetIsReady());
        while (!LocalizationManager.instance.GetIsReady())
        {
            
            yield return null;
        }
        Debug.Log("After while" + LocalizationManager.instance.GetIsReady());
        LocalizationManager.instance.SetIsReady(false);
        Debug.Log("After false" + LocalizationManager.instance.GetIsReady());
        SceneManager.LoadScene(0);
    }
*/
}
